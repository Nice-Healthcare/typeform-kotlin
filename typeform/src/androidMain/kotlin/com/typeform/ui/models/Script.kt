package com.typeform.ui.models

/**
 * Generalized reference to typography styles used in Typeform UI.
 *
 * These align to the Material 3 typography groupings.
 */
enum class Script {
    DISPLAY,
    HEADLINE,
    TITLE,
    BODY,
    LABEL,
}
